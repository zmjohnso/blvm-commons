target/
/target/

# Library crate: lockfile not committed
Cargo.lock

# Workflow logs (large log files from CI)
workflow-logs/

# Test artifacts
test_pattern
*.test

# Local prep / sqlite scratch (never commit)
*.db
