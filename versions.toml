# Org-wide version map (Phase 1 - manual enforcement)

[release_set]
id = "set-2025-01A"

[versions]
blvm-consensus = "v0.1.0-locked"
blvm-protocol  = "v0.1.0"
blvm-node      = "v0.1.0"
blvm-sdk       = "v0.1.0"
governance-app = "v0.1.0"

# Notes:
# - blvm-protocol and blvm-node must depend on the exact blvm-consensus version above
# - Releases should only be cut when all three versions form a consistent set
